export type SignUpFormData = {
    email: string;
    password: string;
    repeatPassword: string;
};

export type SignUpInputName = {
    email: string;
    password: string;
    repeatPassword: string;
};
